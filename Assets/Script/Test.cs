/*using UnityEngine;

public class test : MonoBehaviour
{
    private int n = 1;
    private float timer = 0f; //นาฬิกาจับเวลา เช่น 1 วิผ่านไป รัวลูปกี่ครั้ง

    void Awake()
    {
        Debug.Log("Awake");   
    }

    // Update is called once per frame
    void Start()
    {
        Debug.Log("Start");
    }

    void Update()
    {
        timer += Time.deltaTime;    //+= เพิ่มขึ้นเรื่อยๆ
        n++;

        if (timer >= 1f)
        {
            Debug.Log(n);
            timer = 0f;
        }
    }
}*/
